// Id do usuário que pode ver a página de logs de auditoria (/logs). É uma
// checagem só do lado do frontend — não existe papel de "admin" no backend
// ainda, então isso apenas esconde o link/tela pra quem não é essa conta.
export const USUARIO_ID_LOGS = 14;
